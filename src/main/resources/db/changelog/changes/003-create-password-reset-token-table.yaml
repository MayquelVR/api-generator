databaseChangeLog:
  - changeSet:
      id: 003-create-password-reset-token-table
      author: api-generator
      changes:
        - createTable:
            tableName: password_reset_token
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: email
                  type: varchar(100)
                  constraints:
                    nullable: false
              - column:
                  name: token_hash
                  type: varchar(64)
                  constraints:
                    nullable: false
                    unique: true
              - column:
                  name: expiry_at
                  type: timestamp
                  constraints:
                    nullable: false
        - createIndex:
            indexName: idx_password_reset_token_hash
            tableName: password_reset_token
            columns:
              - column:
                  name: token_hash

